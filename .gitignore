# Dependencies
node_modules/
package-lock.json

# Environment
.env
.env.local
.env.*.local
.dev.vars

# Build output
dist/
*.log

# Wrangler (Cloudflare local state)
.wrangler/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Agent tools
.claude/
.zcode/

# Docker
*.pid

# Local artifacts
last-config.json

# Rust agent (apps/agent) — build output only. Cargo.lock IS committed on
# purpose (application crate: pinned deps — including realm_lb's git rev —
# keep release builds reproducible).
target/
**/*.rs.bk
# Platform link-TLS PEMs written by the rust agent into its working directory
# (secrets — never commit).
apps/agent/certs/
